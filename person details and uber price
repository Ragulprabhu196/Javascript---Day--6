a.
class Person {
  constructor(name, place,age, email) {
    this.name = name;
    this.place = place;
     this.age = age;
    this.email = email;
  }
  }
let pupil = new Person(
  'ARNOLD ',
  'SWITZERLAND',
  '70',
  'Terminator@gmail.com'
);
console.log(pupil);

b.
class ubercost {
    constructor(distance, basefare, time) {
      this.distance = distance;
      this.basefare = basefare;
      this.time = min;
    }
  
    price() {
      let amount;
      if (this.distance > 0 && this.distance < 30) {
        this.rate = 8;
      } else {
        this.rate = 5;
      }
      return ` Time ${this.time} in min.
 fare is  ${this.basefare} 
  and the price = ${this.rate *this.distance -3 *this.time}/-`;
    }
  }
  let traveller= new ubercost(35, 10, 1);
  
  console.log(traveller.price());
